package otter

css labelClass() {
	display: flex;
	flex-direction: column;
	gap: 0.25rem;
	font-weight: 800;
	font-size: 0.875rem;
}

templ Label(label string) {
	<label
		class={ labelClass(), }
	>
		{ label }
		{ children... }
	</label>
}
